{% extends 'base.html.twig' %}

{% block title %}Modification d'une annonce - {{parent()}}{% endblock %}

{% block body %}

	<main class="container">

		<div class="row">

			<div class="col">

				<div class="card border rounded">

					<!-- Entête formulaire de modification d'une annonce -->
					<h2 class="card-header col-8 offset-2 text-center my-3 py-3" >

						Modifier l'annonce intitulée

						<br>
						
						 {{announce.title}}

					</h2>

					<!-- Formulaire de modification -->
					<div class="card-body card-article col-8 offset-2 mb-3 py-3">

						{# On désactive la validation par défaut du navigateur #}
						{{ form_start (form, {'attr': {'novalidate': 'novalidate'}})}}

							{# Affichage des champs titre, description, catégorie, coordonnées de l'entreprise et lien de l'annonce #}
							{{form_widget(form)}}

							{# Affichage des erreurs #}
							<div class="">
								{{ form_errors(form) }}
							</div>

							{# Le bouton de validation de la modification de l'annonce #}
							<button class="btn btn-primary">Confirmer</button>

						{{ form_end (form)}}

					</div>

				</div>

			</div>

		</div>

	</main>

{% endblock %}
